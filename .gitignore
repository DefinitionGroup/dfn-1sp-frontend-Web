node_modules
node_modules
target
dist
logs

.cache
.vscode
.DS_Store
.idea
.next
yarn-error.log
npm-debug.log

node_modules
.DS_Store
# Ignore every env variant (incl. .env.local, backups like .env.local.bak-*) repo-wide…
.env*
# …except the committed templates.
!.env.example
!.env*.example
*.tsbuildinfo
.vercel

# Sanity dataset exports — never commit production data
EXPORT/

# git worktrees managed by Claude Code (never commit as gitlinks)
.claude/worktrees/
